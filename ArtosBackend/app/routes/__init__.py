"""Route blueprints package for API endpoints.

This package will contain Flask blueprints for each route group:
upload, ingest, generate, runs, and search. Each module documents
its endpoint responsibilities and JSON contracts.
"""

